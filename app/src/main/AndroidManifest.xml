<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tdah">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".FirebaseApp"
        android:allowBackup="true"
        android:icon="@drawable/icono"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icono"
        android:supportsRtl="true"
        android:theme="@style/Theme.TDAH">
        <activity
            android:name=".pdf.PerfilPsicologo"
            android:exported="false" />
        <activity
            android:name=".TerminosyCondiciones"
            android:exported="false" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapaCentros"
            android:exported="false"
            android:label="@string/title_activity_mapa_centros" />
        <activity
            android:name=".UsuarioPrincipal"
            android:label="@string/title_activity_menu_usuario"
            android:theme="@style/Theme.TDAH.NoActionBar" />
        <activity
            android:name=".PadrePrincipal"
            android:label="@string/title_activity_menu_usuario"
            android:theme="@style/Theme.TDAH.NoActionBar" />
        <activity
            android:name=".PsicologoPrincipal"
            android:exported="false"
            android:label="@string/title_activity_psicologo_principal"
            android:theme="@style/Theme.TDAH.NoActionBar" />
        <activity
            android:name=".DatosUsuario"
            android:exported="false" />
        <activity
            android:name=".InicioDeSesion"
            android:exported="false" />
        <activity
            android:name=".RegistroUsuario"
            android:exported="false" />
        <activity android:name=".usuario.Oro" />
        <activity android:name=".usuario.Escenario" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>